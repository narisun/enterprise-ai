# Agent orchestration
langgraph>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.3.0

# API server
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.7.0,<3.0.0

# Prompt templating
jinja2>=3.1.0,<4.0.0

# Observability (shared with platform-sdk but pinned here for the container)
opentelemetry-sdk>=1.24.0,<2.0.0
opentelemetry-exporter-otlp>=1.24.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.45b0,<1.0.0

# LLM observability (must match platform-sdk dependency)
langfuse>=2.0.0,<3.0.0

# Test dependencies live in requirements-test.txt — never install them in Docker images.
