"""Service layer — orchestration code that depends on Protocol ports.

ChatService owns the per-request graph invocation, streaming, and
persistence flow. Constructor-injected; no module-level state.
"""
