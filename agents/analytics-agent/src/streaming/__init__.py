"""Streaming wire-format encoders — translate domain events into bytes.

Currently exports DataStreamEncoder (Vercel AI SDK Data Stream Protocol).
A future SSEEncoder can satisfy the same StreamEncoder Protocol.
"""
