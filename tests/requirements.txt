# Test-layer dependencies
# Install alongside each service's own requirements:
#   pip install -e platform-sdk/
#   pip install -r tests/requirements.txt
#
# The platform-sdk is the only non-test dep needed for unit tests —
# it provides AgentContext, make_cache_key, RMBrief, etc.

# Core test framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0
pytest-cov>=5.0.0
# Automatically loads .env before tests run (does NOT override env vars already
# set by the shell or CI — safe for both local dev and GitHub Actions).
pytest-dotenv>=0.5.2

# HTTP client for integration + eval tests
httpx>=0.27.0

# DB driver for integration tests (direct SQL)
asyncpg>=0.29.0

# JWT for test token generation
pyjwt>=2.8.0

# OpenAI SDK used by the LLM judge in eval tests
openai>=1.30.0

# Structured output parsing in eval tests
pydantic>=2.7.0

# ── RAGAS faithfulness metric ────────────────────────────────────────────────
# ragas 0.2.x API — uses LangchainLLMWrapper + EvaluationDataset
# Pin to <0.3 to avoid pulling in langchain 1.x / langgraph 1.x which
# conflict with the project's langgraph 0.2.x + langchain-core 0.2.x pins.
ragas>=0.2.0,<0.3.0
# HuggingFace datasets still needed for result inspection helpers
datasets>=2.20.0

# ── Guard pins ────────────────────────────────────────────────────────────────
# These prevent ragas/datasets transitive deps from upgrading to incompatible versions.
langchain-core>=0.2.0,<0.3.0
langchain-openai>=0.1.8,<0.3.0
langgraph>=0.2.0,<0.3.0
