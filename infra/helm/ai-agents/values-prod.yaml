replicaCount: 3
hpa:
  minReplicas: 3
  maxReplicas: 12
ingress:
  enabled: true
  scheme: internet-facing
  host: "api.enterprise-ai.example.com"       # Replace with your actual domain
  certificateArn: "arn:aws:acm:us-east-1:ACCOUNT:certificate/REPLACE_ME"
  accessLogsBucket: "enterprise-ai-alb-logs"  # Create this S3 bucket before first deploy
  wafAclArn: ""                               # Add WAFv2 ARN when available
