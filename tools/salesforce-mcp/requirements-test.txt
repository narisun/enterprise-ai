# Test-only dependencies for tools/salesforce-mcp/
# Install alongside the production deps:
#   pip install -r tools/salesforce-mcp/requirements.txt -r tools/salesforce-mcp/requirements-test.txt
# Never COPY or RUN this in a Dockerfile.

pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<6.0.0
